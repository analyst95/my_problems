#include<stdio.h>
#include<string.h>
int main()
{	char word[j],line[i];
	int i=0,j=0,move=0;
	scanf("%s\n",word);
	scanf("%s\n",line);
	
	while((word[i]==line[j])&&(j!=strlen(word)))
	{
		i++;
		j++;

	}
	
    
	return 0;
}